const rowStyle = {
    width: '100%',
    display: 'flex',
    flexFlow: 'row wrap',
  };
  const colStyle = {
    marginBottom: '28px',
  };
  const gutter = 36;


  export {
      rowStyle,
      colStyle,
      gutter
  }